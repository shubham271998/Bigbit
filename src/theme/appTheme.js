import palette from './palette';
import {typography} from './typography';

export const APP_THEME = {
  palette,
  typography,
  primaryColor: palette.tiffany_blue,
  secondaryColor: palette.blue_violet,
};
